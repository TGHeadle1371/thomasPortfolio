$(document).ready(function() {
    $(".fixed-action-btn").floatingActionButton({
        direction: "left",
        hoverEnabled: false
    });

    $(".parallax").parallax();
    $(".sidenav").sidenav();
});
